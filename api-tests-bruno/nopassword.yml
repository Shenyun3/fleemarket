info:
  name: nopassword
  type: http

http:
  method: POST
  url: http://localhost:3000/api/auth/signup
  body:
    type: json
    data: |-
      {
        "email": "test-user2@example.com",
        "username": "coder_bian"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
