info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/auth/signup
  body:
    type: json
    data: |-
      {
        "email": "test@example.com",
        "password": "SecurePassword-123",
        "username": "bianhanzhang"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
